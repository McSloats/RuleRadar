# Core monitor
pyyaml>=6.0,<7.0
tomli>=2.0,<3.0; python_version < "3.11"  # TOML parsing for Elastic rules (stdlib tomllib on 3.11+)

# Web interface + auth
flask>=3.0,<4.0
flask-login>=0.6,<1.0
flask-wtf>=1.2,<2.0
bcrypt>=4.0,<5.0

# Scheduler (replaces OS cron inside Docker / locally)
APScheduler>=3.10,<4.0

# Timezone support (zoneinfo is stdlib on 3.9+; backport for 3.8)
tzdata>=2024.1
backports.zoneinfo>=0.2.1; python_version < "3.9"
