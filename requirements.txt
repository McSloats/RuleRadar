# Core monitor
pyyaml>=6.0
tomli>=2.0,<3.0; python_version < "3.11"  # TOML parsing for Elastic rules (stdlib tomllib on 3.11+)

# Web interface + auth
flask>=3.0,<4.0
flask-login>=0.6,<1.0
flask-wtf>=1.2,<2.0
bcrypt>=4.0                                # no upper cap — 5.x required for Python 3.14+

# Scheduler (replaces OS cron inside Docker / locally)
# Pinned to 3.x — APScheduler 4.x has a completely different API
APScheduler>=3.10,<4.0

# Timezone support (zoneinfo is stdlib since 3.9; tzdata provides IANA data on all platforms)
tzdata>=2025.1
